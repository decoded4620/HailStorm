
task sourcesJar(type: Jar) {
    from sourceSets.main.allJava
    archiveClassifier.set("sources")
}

task javadocJar(type: Jar) {
    from javadoc
    archiveClassifier.set("javadoc")
}

task writeNewPom {
    doLast {
        pom {
            project {
                inceptionYear '2019'
                licenses {
                    license {
                        name 'The Apache Software License, Version 2.0'
                        url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                        distribution 'repo'
                    }
                }
            }
        }.writeTo("./pom.xml")
    }
}

// maven plugin
publishing {
    // TODO @barcher setup artifactory
    publications {
        maven(MavenPublication) {
            groupId = 'com.decoded'
            artifactId = 'HailStorm'
            version = '1.0'

            from components.java
        }

        mavenJava(MavenPublication) {
            artifactId = 'HailStorm'
            from components.java
            artifact sourcesJar
            artifact javadocJar
            pom {
                name = 'HailStorm'
                description = 'Distributed Globally Unique Id generator for Java Hosts'
                url = 'TODO - SET THIS URL'

                licenses {
                    license {
                        name = 'Apache 2.0'
                        url = 'TODO'
                    }
                }
                developers {
                    developer {
                        id = 'decoded4620'
                        name = 'Decoded LLC'
                        email = 'decoded4620@github'
                    }
                }
                scm {
                    connection = 'scm:git:git://github.com/decoded4620/HailStorm.git'
                    developerConnection = 'scm:git:ssh://github.com/decoded4620/HailStorm.git'
                    url = 'https://github.com/decoded4620/HailStorm/'
                }
            }
        }
    }
}
